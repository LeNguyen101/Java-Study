public class WritingMethods {
    public static double distance
          (double x1, double y1, double x2,double y2) {
      double dx = x2 - x1;
      double dy = y2 - y1;
      double dsquare = dx * dx + dy * dy;
      System.out.println("dsquared is " + dsquare);
      return 0.0;
    }
}